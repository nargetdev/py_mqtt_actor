session_name: mqtt-actor-test
windows:
  - window_name: allstuffs
    layout: even-vertical
    panes:
      - shell_command:
          - docker compose up --force-recreate emqx
      - shell_command:
          - docker compose up --force-recreate function-based-actor
      - shell_command:
          - docker compose up --force-recreate test-function-based-actor
      - shell_command:
          - docker compose up --force-recreate resp-subscriber
      - shell_command:
          - docker compose up --force-recreate sync-subscriber
      - shell_command:
        - docker compose up --force-recreate emoji-resp-subscriber